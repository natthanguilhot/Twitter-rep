if tweets
  each tweet in tweets
    div(class="card tweet-element")
      div(class="card-body")
      .card-text= tweet.content
      button(class="btn btn-danger mt-2 float-right" tweetid=tweet.id) x